let foo = {
  let x = 0;
  ++x;
  x + 2;
};

let foo = { x: 2 };
let bar = { ...foo, y: 4 };

let arr1 = [1, 2, 3];
let arr2 = [4, ...arr1, 5];

const cb1 = (foo: number) => { x: 2, foo };
const cb2 = (foo: number) => {
  foo + 2;
};

{ foo, bar } = someCall();
