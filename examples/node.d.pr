declare global const process: Process;

declare global type Process = {
  env: Record{Envars, string?},
  argv: string[] where {
    length: number > 2,
  },
};

/// Convenience type for overriding available environment variable keys.
declare global type Envars < string;
